# Server-only HTTP(S) origin used by the allowlisted Next.js route handler.
# Use an origin only: no credentials, path, query or fragment. Never rename this
# to NEXT_PUBLIC_* and never put a private filesystem path in this file.
IVOIREVOICE_API_INTERNAL_URL=http://127.0.0.1:8000
